{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.py ]] && /data/FASTDelphos/.venv/bin/ruff format \"$f\" && /data/FASTDelphos/.venv/bin/ruff check --fix \"$f\"; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Formatting with ruff..."
          }
        ]
      }
    ]
  }
}
